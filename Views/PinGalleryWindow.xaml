<Window
    x:Class="Pinshot.Views.PinGalleryWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="贴图列表"
    Width="480"
    Height="460"
    Background="{StaticResource WindowBackgroundBrush}"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="GalleryIconButton" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="PART_Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Bd" Property="Background" Value="{StaticResource ControlHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Bd" Property="Background" Value="{StaticResource ControlPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel>
        <!--  标题行  -->
        <DockPanel DockPanel.Dock="Top" Margin="14,10,10,6">
            <Button
                x:Name="PART_ClearButton"
                Width="28"
                Height="24"
                Background="Transparent"
                BorderThickness="0"
                Click="OnClear"
                Content="🗑"
                Cursor="Hand"
                DockPanel.Dock="Right"
                FontSize="13"
                Style="{StaticResource GalleryIconButton}"
                ToolTip="清空回收站" />
            <TextBlock
                VerticalAlignment="Center"
                FontSize="13.5"
                Foreground="{StaticResource TextSecondaryBrush}"
                Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />
        </DockPanel>

        <!--  列表  -->
        <ListBox
            x:Name="PART_List"
            Margin="8,0,8,6"
            ItemContainerStyle="{StaticResource RowItemStyle}"
            MouseDoubleClick="OnListDoubleClick"
            SelectionMode="Single"
            Style="{StaticResource PlainListBoxStyle}" />

    </DockPanel>
</Window>
